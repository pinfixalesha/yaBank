spring:
  application:
    name: eurekaServiceDiscovery
server:
  port: ${EUREKA_SERVER_PORT}
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false